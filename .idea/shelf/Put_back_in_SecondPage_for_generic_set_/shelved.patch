Index: blamo/lib/Export/index.dart
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import 'package:blamo/main.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n\r\n/*This page will serve as a manual export option all different avenues\r\n *\r\n */\r\nclass ExportPage extends StatefulWidget {\r\n  @override\r\n  _ExportPageState createState() => _ExportPageState();\r\n}\r\n\r\nclass _ExportPageState extends State<ExportPage> {\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    return new Scaffold(\r\n      drawer: new Drawer(\r\n        child: SideMenu(),\r\n      ),\r\n      appBar: new AppBar(\r\n          title: new Text(\"Export\"),\r\n          actions: <Widget>[\r\n          ],\r\n          backgroundColor: Colors.deepOrangeAccent\r\n      ),\r\n      body: Center(\r\n        child: Center(child:_exportList()),\r\n      ),\r\n    );\r\n  }\r\n}\r\nWidget _exportList() => ListView(\r\n  children: [\r\n    Center(child:_tile('Email',Icons.email)),\r\n    Divider(),\r\n    Center(child:_tile('Box',Icons.cloud_upload))\r\n  ],\r\n);\r\nListTile _tile(String destination, IconData icon) => ListTile(\r\n  title: Text(destination,\r\n      style: TextStyle(\r\n          fontWeight: FontWeight.w600,\r\n          fontSize:25\r\n      )),\r\n  leading: Padding(\r\n    padding: const EdgeInsets.all(20.0),\r\n    child: Icon(\r\n      icon,\r\n      color: Colors.blue,\r\n    ),\r\n  ),\r\n);\r\n\r\n//TODO Add functionality for onclick to grab CSV and email, text popup for email to send to\r\n//TODO cloud save to Box(Both form AND CSV?)
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- blamo/lib/Export/index.dart	(date 1580190844000)
+++ blamo/lib/Export/index.dart	(date 1580193848244)
@@ -1,6 +1,7 @@
 import 'package:blamo/main.dart';
 import 'package:flutter/material.dart';
-
+import 'package:flutter/services.dart';
+import 'dart:async';
 
 /*This page will serve as a manual export option all different avenues
  *
@@ -52,4 +53,47 @@
 );
 
 //TODO Add functionality for onclick to grab CSV and email, text popup for email to send to
+/* Sets up async email send wtih CSV file, taken from flutter_email_sender package repo 
+ * https://github.com/sidlatau/flutter_email_sender/blob/master/lib/flutter_email_sender.dart
+ *
+ */
+class FlutterEmailSend {
+  static const MethodChannel _channel =
+      const MethodChannel('flutter_email_sender');
+  static Future<void> send(Email mail) {
+    return _channel.invokeMethod(('send', mail.toJson());
+  }
+}
+class Email {
+  final String subject;
+  final List<String> recipients;
+  final List<String> cc;
+  final List<String> bcc;
+  final String body;
+  final String attachmentPath;
+  final bool isHTML;
+  Email({
+    this.subject ='',
+    this.recipients = const[],
+    this.cc = const[],
+    this.bcc = const [],
+    this.body = '',
+    this.attachmentPath,
+    this.isHTML = false,
+  });
+  Map<String, dynamic> toJson() {
+    return {
+      'subject': subject,
+      'body': body,
+      'recipients' : recipients,
+      'cc' : cc,
+      'bcc' : bcc,
+      'attachment_path' : attachmentPath,
+      'is_html' : isHTML
+    };
+}
+void emailSend() async {
+
+}
+
 //TODO cloud save to Box(Both form AND CSV?)
\ No newline at end of file
